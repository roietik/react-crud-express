{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "node-sass": "^4.13.1",
    "normalize.scss": "^0.1.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "^3.4.0"
  },
  "scripts": {
    "watch:sass": "node-sass -o public/css src/sass/styles.scss -w",
    "start": "react-scripts start watch:sass",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "proxy": "http://localhost:5000",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
